services:
  regiq:
    build: .
    container_name: regiq
    ports:
      - "80:8000"
    env_file:
      - .env
    environment:
      - REGIQ_API_BASE_URL=http://localhost:8000
      - API_HOST=0.0.0.0
      - API_PORT=8000
    restart: unless-stopped
